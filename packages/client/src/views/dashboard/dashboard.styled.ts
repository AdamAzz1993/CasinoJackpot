import styled from 'styled-components';

export const DashboardContainer = styled.div`
display: flex;
gap: 3rem;
flex-direction: column;
color: black;

.blocks {
   display: flex;
   gap: 3rem;
   flex-direction: row;
   color: black;
}`;
